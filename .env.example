# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# ===========================================
# LLM Provider Configuration (REQUIRED)
# ===========================================

# Choose your LLM provider (REQUIRED)
LLM_PROVIDER=openai

# Model IDs (REQUIRED) - these are the actual model IDs sent to the API
LLM_CHAT_MODEL=gpt-4o
LLM_REASONING_MODEL=o1

# Display names for UI (optional - defaults to model ID)
LLM_CHAT_MODEL_NAME=GPT-4o
LLM_REASONING_MODEL_NAME=OpenAI o1

# Client-side env vars (NEXT_PUBLIC_ prefix required for browser access)
# These must match the server-side values above
NEXT_PUBLIC_LLM_PROVIDER=openai
NEXT_PUBLIC_LLM_CHAT_MODEL=gpt-4o
NEXT_PUBLIC_LLM_REASONING_MODEL=o1
NEXT_PUBLIC_LLM_CHAT_MODEL_NAME=GPT-4o
NEXT_PUBLIC_LLM_REASONING_MODEL_NAME=OpenAI o1

# Optional: Models for other features (defaults to LLM_CHAT_MODEL)
# LLM_TITLE_MODEL=gpt-4o-mini
# LLM_ARTIFACT_MODEL=gpt-4o

# --- OpenAI Configuration ---
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# --- Anthropic Configuration ---
# Get your API key from: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=sk-ant-...

# --- Google AI Configuration ---
# Get your API key from: https://aistudio.google.com/apikey
# GOOGLE_API_KEY=...

# ===========================================
# Example configurations for each provider:
# ===========================================
#
# OpenAI:
#   LLM_PROVIDER=openai
#   LLM_CHAT_MODEL=gpt-4o
#   LLM_REASONING_MODEL=o1
#   LLM_CHAT_MODEL_NAME=GPT-4o
#   LLM_REASONING_MODEL_NAME=OpenAI o1
#
# Anthropic:
#   LLM_PROVIDER=anthropic
#   LLM_CHAT_MODEL=claude-sonnet-4-5-20250514
#   LLM_REASONING_MODEL=claude-sonnet-4-5-20250514
#   LLM_CHAT_MODEL_NAME=Claude Sonnet 4.5
#   LLM_REASONING_MODEL_NAME=Claude Sonnet 4.5
#
# Google:
#   LLM_PROVIDER=google
#   LLM_CHAT_MODEL=gemini-2.5-flash
#   LLM_REASONING_MODEL=gemini-2.5-pro
#   LLM_CHAT_MODEL_NAME=Gemini 2.5 Flash
#   LLM_REASONING_MODEL_NAME=Gemini 2.5 Pro

# ===========================================
# Infrastructure Configuration
# ===========================================

# Instructions to create a Vercel Blob Store here: https://vercel.com/docs/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# Instructions to create a PostgreSQL database here: https://vercel.com/docs/postgres
# Supported providers: Supabase, Neon, Railway, Prisma
POSTGRES_URL=****

# Instructions to create a Redis store here:
# https://vercel.com/docs/redis
REDIS_URL=****
